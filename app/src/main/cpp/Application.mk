# Note: APP_ABI is ignored by Gradle's externalNativeBuild.
# Instead, use an abiFilters block or (if you are using "Multiple APKs") an abi block inside a splits block.
APP_STL := c++_static
APP_ABI := arm64-v8a x86_64